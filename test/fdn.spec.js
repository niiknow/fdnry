import chai from 'chai';
import Feedinary from '../src/feedinary.js';

chai.expect();

const expect = chai.expect;

var lib;

describe('Given an instance of Feedinary', function () {
  before(function () {
    lib = new Feedinary();
  });
  describe('when I need to init', function () {

  });
});
